const userRoot = "/user"

const signRoot = "/sign"

const SIGN = {
  UP: `${userRoot}${signRoot}/up1`,
  IN: `${userRoot}${signRoot}/in1`,
  OUT: `${userRoot}${signRoot}/out1`,
}

export const UserService = {
  INFO: `${userRoot}/info`,
  SIGN,
}
